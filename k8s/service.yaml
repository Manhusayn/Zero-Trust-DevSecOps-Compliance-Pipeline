apiVersion: v1
kind: Service
metadata:
  name: zero-trust-demo
  namespace: zero-trust-demo
spec:
  selector:
    app: zero-trust-demo
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
